[deploy]
startCommand = "python3 -c \"import http.server,os,socketserver; socketserver.TCPServer(('',int(os.environ.get('PORT',8080))),http.server.BaseHTTPRequestHandler).serve_forever()\""
buildCommand = "cd backend && pip install -r requirements.txt && python manage.py collectstatic --noinput"
